import type { AuditAnalysis, PageAudit, PageSpeedSummary, SiteScrape } from './types';
import { env } from './env';

const e = (v: unknown) => String(v ?? '').replace(/[&<>"']/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;' }[c]!));
const clamp = (n: number) => Math.max(0, Math.min(100, Math.round(Number(n) || 0)));
function compactText(value: unknown, maxChars: number) {
  const text = String(value ?? '').replace(/\s+/g, ' ').trim();
  if (text.length <= maxChars) return text;
  const slice = text.slice(0, maxChars - 1);
  const sentence = Math.max(slice.lastIndexOf('. '), slice.lastIndexOf('! '), slice.lastIndexOf('? '));
  const word = slice.lastIndexOf(' ');
  const cut = sentence > maxChars * .55 ? sentence + 1 : word > maxChars * .7 ? word : slice.length;
  return slice.slice(0, cut).trimEnd() + '…';
}

function hostFor(url: string) { try { return new URL(url).hostname.replace(/^www\./, ''); } catch { return url; } }
function pathFor(url: string) { try { return new URL(url).pathname || '/'; } catch { return url; } }
function displayPage(url: string) {
  const p = pathFor(url);
  if (p === '/') return 'Homepage';
  const last = p.split('/').filter(Boolean).pop() || p;
  return last.replace(/[-_]+/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
}
function brandNameHtml() {
  const name = env.REPORT_BRAND_NAME || 'MsSaraJo';
  if (name.toLowerCase() === 'mssarajo') return '<span class="brand-ms">Ms</span><span class="brand-sara">Sara</span><span class="brand-jo">Jo</span>';
  return e(name);
}
function sparkle(className = '') {
  return `<svg class="spark-svg ${className}" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 0C12.7 7.2 16.8 11.3 24 12C16.8 12.7 12.7 16.8 12 24C11.3 16.8 7.2 12.7 0 12C7.2 11.3 11.3 7.2 12 0Z"/></svg>`;
}
function polarPoint(cx: number, cy: number, radius: number, angleDeg: number) {
  const a = angleDeg * Math.PI / 180;
  return { x: cx + radius * Math.cos(a), y: cy + radius * Math.sin(a) };
}
function arcPath(cx: number, cy: number, radius: number, startAngle: number, sweepAngle: number) {
  const start = polarPoint(cx, cy, radius, startAngle);
  const end = polarPoint(cx, cy, radius, startAngle + sweepAngle);
  return `M ${start.x.toFixed(2)} ${start.y.toFixed(2)} A ${radius} ${radius} 0 ${sweepAngle > 180 ? 1 : 0} 1 ${end.x.toFixed(2)} ${end.y.toFixed(2)}`;
}
function scoreGauge(value: number, hero = false) {
  const score = clamp(value), cx = 60, cy = 60, radius = hero ? 47 : 42, startAngle = 210, maxSweep = 240;
  const sweep = Math.max(.01, maxSweep * score / 100);
  const start = polarPoint(cx, cy, radius, startAngle);
  const end = polarPoint(cx, cy, radius, startAngle + sweep);
  const companion = polarPoint(cx, cy, radius + (hero ? 8 : 5), startAngle + sweep + 8);
  const ornate = 'M0 -9C.8-2.9 2.9-.8 9 0C2.9.8.8 2.9 0 9C-.8 2.9-2.9.8-9 0C-2.9-.8-.8-2.9 0-9Z';
  return `<svg class="score-gauge ${hero ? 'hero' : ''}" viewBox="0 0 120 120" aria-hidden="true">
    ${hero ? `<path class="gauge-track" d="${arcPath(cx,cy,radius,startAngle,maxSweep)}"/>` : ''}
    <path class="gauge-progress" d="${arcPath(cx,cy,radius,startAngle,sweep)}"/>
    <g transform="translate(${start.x.toFixed(2)} ${start.y.toFixed(2)}) scale(${hero ? .42 : .28})"><path d="${ornate}"/></g>
    <g transform="translate(${end.x.toFixed(2)} ${end.y.toFixed(2)}) scale(${hero ? 1 : .65})"><path d="${ornate}"/></g>
    <g class="gauge-companion" transform="translate(${companion.x.toFixed(2)} ${companion.y.toFixed(2)}) scale(${hero ? .28 : .2})"><path d="M0 -7L1.8-1.8L7 0L1.8 1.8L0 7L-1.8 1.8L-7 0L-1.8-1.8Z"/></g>
  </svg>`;
}
function notchedFrame(className = '') {
  const ratios: Record<string, number> = {
    'executive-frame': 3.2, 'focus-frame': 3.05, 'page-card-frame': 1.75,
    'insight-frame': 1.45, 'why-frame': 4.8, 'rewrite-frame': 1.55,
    'takeaway-frame': 4.8, 'method-frame': 4.8,
  };
  const ratio = ratios[className] || 3;
  const h = 100, w = Math.round(ratio * h), n = 8;
  return `<svg class="notched-frame ${className}" viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" aria-hidden="true"><path d="M${n} 0H${w-n}L${w} ${n}V${h-n}L${w-n} ${h}H${n}L0 ${h-n}V${n}Z"/></svg>`;
}
function decorativeArc(className='') { return `<svg class="decorative-arc ${className}" viewBox="0 0 100 100" aria-hidden="true"><path d="M8 88A72 72 0 0 0 91 11"/></svg>`; }
function brandLockup() {
  return `<div class="brand-lockup"><div class="brand-icon"><svg viewBox="0 0 62 48" aria-hidden="true"><rect x="4" y="6" width="40" height="32" rx="5" fill="none" stroke="#17213A" stroke-width="1.8"/><path d="M4 13h40" stroke="#17213A" stroke-width="1.5"/><circle cx="9" cy="9.6" r="1.2" fill="#C97962"/><circle cx="13.5" cy="9.6" r="1.2" fill="#C97962"/><circle cx="18" cy="9.6" r="1.2" fill="#C97962"/><rect x="9" y="18" width="9" height="8" rx="1.5" fill="#E5AA97"/><path d="M23 20h13M23 24h10" stroke="#8A8792" stroke-width="1.4"/><path d="M9 33l7-5 6 2 7-7" fill="none" stroke="#C97962" stroke-width="1.8"/><path d="M37 26l12 12 2.5-7.3 6.5-2.7-21-7z" fill="#FCF9F4" stroke="#17213A" stroke-width="1.8"/></svg>${sparkle('brand-spark')}</div><div><div class="brand-name">${brandNameHtml()}</div><div class="brand-subtitle">Website insight studio</div></div></div>`;
}
function topbar(label: string) { return `<div class="topbar">${brandLockup()}<div class="review-pill">${sparkle('mini-spark')}${e(label)}</div></div>`; }
function footer(domain: string, date: string, page: number, total: number) {
  return `<footer><div class="footer-rule"><span></span>${sparkle('footer-spark')}<span></span></div><div class="footer-meta"><span>${e(domain)}</span><span>PAGE ${page} OF ${total}</span><span>${e(date)}</span></div></footer>`;
}
function scoreStatus(value: number) {
  if (value >= 80) return { label:'Excellent', cls:'excellent' };
  if (value >= 75) return { label:'Strong', cls:'strong' };
  if (value >= 65) return { label:'Good foundation', cls:'foundation' };
  return { label:'Opportunity', cls:'opportunity' };
}
function pageInsightFallback(page: PageAudit, analysis: AuditAnalysis, index: number) {
  const pageActions = analysis.actionItems.filter(a => a.pageUrl && pathFor(a.pageUrl) === pathFor(page.url));
  const good = pageActions.find(a => a.severity === 'good');
  const issue = pageActions.find(a => a.severity !== 'good');
  const scoreDelta = pageActions.reduce((n,a)=>n + (a.severity === 'critical' ? -5 : a.severity === 'warning' ? -2 : 2), 0);
  return {
    pageUrl: page.url,
    pageLabel: displayPage(page.url),
    role: compactText(index === 0 ? 'Primary entry page and first impression' : `Supports the customer journey through ${displayPage(page.url).toLowerCase()}`, 150),
    score: clamp(analysis.overallScore + scoreDelta),
    strength: compactText(good?.title || (page.h1[0] ? `Clear primary heading: “${page.h1[0]}”` : 'Useful content foundation is present.'), 245),
    opportunity: compactText(issue?.title || (page.imagesMissingAlt ? `${page.imagesMissingAlt} image${page.imagesMissingAlt === 1 ? '' : 's'} need alt text.` : 'Refine the page structure and conversion cues.'), 245),
  };
}
function pageInsights(analysis: AuditAnalysis, site: SiteScrape) {
  const supplied = (analysis.pageInsights || []).slice(0,5).map(x => ({
    ...x,
    pageLabel: compactText(x.pageLabel, 34), role: compactText(x.role, 150),
    strength: compactText(x.strength, 245), opportunity: compactText(x.opportunity, 245),
  }));
  if (supplied.length) return supplied;
  return site.pages.slice(0,5).map((p,i)=>pageInsightFallback(p,analysis,i));
}
function crossInsights(analysis: AuditAnalysis, site: SiteScrape) {
  const supplied = (analysis.crossPageInsights || []).slice(0,4).map(x => ({
    ...x, title: compactText(x.title, 52), finding: compactText(x.finding, 330), why: compactText(x.why, 210),
  }));
  if (supplied.length) return supplied;
  const grouped = ['SEO','UX','Performance','Accessibility'].map(category => {
    const items = analysis.actionItems.filter(a=>a.category === category).slice(0,3);
    const first = items[0];
    return {
      title: category === 'UX' ? 'Conversion flow' : category === 'SEO' ? 'Search visibility' : category === 'Performance' ? 'Performance balance' : 'Accessibility consistency',
      finding: first ? first.why : `Review ${category.toLowerCase()} patterns across the most important pages rather than treating each page in isolation.`,
      why: first?.how || 'Consistency across pages makes the customer journey easier to understand and act on.',
      pageUrls: items.map(x=>x.pageUrl).filter(Boolean) as string[],
    };
  });
  return grouped.map(x=>({...x,pageUrls:x.pageUrls.length?x.pageUrls:site.pages.slice(0,3).map(p=>p.url)}));
}
function pageSpeedFor(url: string, pageSpeed: PageSpeedSummary[]) {
  return pageSpeed.find(p=>pathFor(p.url)===pathFor(url)) || pageSpeed[0];
}

export function renderComprehensiveReportHtml(input: { analysis: AuditAnalysis; site: SiteScrape; pageSpeed: PageSpeedSummary[]; createdAt: string }) {
  const { analysis, site, pageSpeed } = input;
  const domain = hostFor(site.startUrl);
  const preparedDate = new Date(input.createdAt).toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'});
  const insights = pageInsights(analysis, site);
  const cross = crossInsights(analysis, site);
  const topActions = analysis.actionItems.filter(a=>a.severity !== 'good').slice(0,4);
  const rewrites = analysis.rewrites.slice(0,5);
  const totalPages = 7;
  const categoryScores = [
    ['Performance', analysis.scores.performance], ['SEO', analysis.scores.seo], ['User experience', analysis.scores.ux], ['Accessibility', analysis.scores.accessibility], ['AI readiness', analysis.scores.geo],
  ] as const;

  const scoreCards = categoryScores.map(([label,value])=>{ const s=scoreStatus(value); return `<div class="score-card ${s.cls}"><div class="score-orbit">${scoreGauge(value)}<strong>${value}</strong></div><span class="score-card-label">${e(label)}</span><span class="score-card-status"><i></i>${e(s.label)}</span></div>`; }).join('');
  const chips = insights.map(x=>`<span class="page-chip">${e(x.pageLabel)}</span>`).join('');

  const pageCards = insights.map((x,i)=>{ const s=scoreStatus(x.score); return `<article class="page-card ${i===0?'featured':''}">${notchedFrame('page-card-frame')}<div class="page-card-head"><div><span class="page-card-kicker">${e(x.pageLabel)}</span><h3>${e(x.pageLabel)} Review</h3><p>${e(x.role)}</p></div><div class="mini-gauge ${s.cls}">${scoreGauge(x.score)}<strong>${x.score}</strong></div></div><div class="page-card-body"><div><span>What's working</span><p>${e(x.strength)}</p></div><div><span>Primary opportunity</span><p>${e(x.opportunity)}</p></div></div><div class="page-card-status ${s.cls}"><i></i>${e(s.label)}</div></article>`; }).join('');

  const insightCards = cross.map(x=>`<article class="insight-card">${notchedFrame('insight-frame')}<div class="insight-title">${sparkle('box-spark')}<span>${e(x.title)}</span></div><p>${e(x.finding)}</p><div class="insight-meta"><div><strong>Why it matters</strong><span>${e(x.why)}</span></div><div><strong>Pages affected</strong><span>${e((x.pageUrls||[]).map(displayPage).join(', ') || 'Multiple pages')}</span></div></div></article>`).join('');

  const roadmap = topActions.map((a,i)=>`<article class="roadmap-item"><div class="roadmap-number">${String(i+1).padStart(2,'0')}</div><div class="roadmap-content"><span class="roadmap-status ${a.impact==='high'?'impact':'improve'}">${sparkle('status-spark')}${a.impact==='high'?'High impact':'Improve next'}</span><h3>${e(compactText(a.title, 82))}</h3><div class="roadmap-cols"><div><strong>Why this matters</strong><p>${e(compactText(a.why, 245))}</p></div><div><strong>Recommended next step</strong><p>${e(compactText(a.how, 270))}</p></div></div><div class="roadmap-meta"><span>Focus area: ${e(a.category)}</span><span>${a.pageUrl?`Pages: ${e(displayPage(a.pageUrl))}`:`Effort: ${e(a.effort)}`}</span></div></div><div class="roadmap-art">${decorativeArc('roadmap-arc')}${sparkle('roadmap-star one')}${sparkle('roadmap-star two')}${sparkle('roadmap-star three')}</div></article>`).join('');

  const rewriteCards = rewrites.map((r,i)=>`<article class="rewrite-card">${notchedFrame('rewrite-frame')}<div class="rewrite-head"><span>${String(i+1).padStart(2,'0')}</span><div><small>${e(displayPage(r.pageUrl))}</small><h3>Ready-to-use copy</h3></div>${sparkle('rewrite-spark')}</div><div class="rewrite-grid"><div><strong>Search title</strong><p>${e(compactText(r.title, 100))}</p></div><div><strong>Meta description</strong><p>${e(compactText(r.metaDescription, 175))}</p></div><div><strong>Primary heading</strong><p>${e(compactText(r.primaryH1, 120))}</p></div><div><strong>Primary CTA</strong><p>${e(compactText(r.cta, 70))}</p></div></div></article>`).join('');

  const speedRows = insights.map(x=>{ const p=pageSpeedFor(x.pageUrl,pageSpeed); return `<tr><td>${e(x.pageLabel)}</td><td>${e(p?.strategy || '—')}</td><td>${p?.scores.performance ?? '&mdash;'}</td><td>${p?.scores.accessibility ?? '&mdash;'}</td><td>${p?.scores.seo ?? '&mdash;'}</td></tr>`; }).join('');

  const pages = [
`<section class="report-page cover-page">${topbar('In-depth website review')}<div class="prepared-line">Prepared for ${e(domain)}</div><div class="cover-grid"><div><div class="script-accent">In-Depth</div><h1>Comprehensive<br/>Website Audit</h1><div class="ornament-rule"><span></span>${sparkle('ornament-spark')}<span></span></div><p class="cover-subtitle">A strategic review of up to five priority pages, designed to uncover how your site performs across search visibility, user experience, conversion flow, accessibility, and AI readiness.</p><div class="site-url">${e(site.startUrl)}</div><div class="cover-stats"><div><strong>${insights.length}</strong><span>Key pages reviewed</span></div><div><strong>${topActions.length}</strong><span>Priority actions</span></div><div><strong>${analysis.overallScore}</strong><span>Overall score</span></div></div></div><div class="cover-side"><div class="hero-score">${scoreGauge(analysis.overallScore,true)}<strong>${analysis.overallScore}</strong></div><span class="hero-score-label">Overall<br/>website score</span><div class="seal"><span>Key pages</span><b>${insights.length}</b><span>reviewed</span></div></div></div><div class="executive-box">${notchedFrame('executive-frame')}<div class="box-heading">${sparkle('box-spark')}Executive summary</div><p>${e(compactText(analysis.executiveSummary, 760))}</p><div class="executive-stars">${sparkle('large')}${sparkle('small navy')}${sparkle('tiny')}</div></div>${footer(domain,preparedDate,1,totalPages)}</section>`,
`<section class="report-page glance-page">${topbar('Comprehensive tier')}<div class="prepared-line">Prepared for ${e(domain)}</div><h2>Your Site at a Glance</h2><p class="hero-subhead">A high-level snapshot of how the full website experience is performing across the areas that matter most.</p><div class="chip-row">${chips}</div><div class="score-legend"><span class="excellent"><i></i>Excellent</span><span class="strong"><i></i>Strong</span><span class="foundation"><i></i>Good foundation</span><span class="opportunity"><i></i>Opportunity</span></div><div class="score-grid">${scoreCards}</div><div class="focus-panel">${notchedFrame('focus-frame')}<div class="focus-title">Where the<br/>biggest gains are</div><ol>${analysis.quickWins.slice(0,3).map((q,i)=>`<li><span>${i+1}</span><div><strong>${e(compactText(q, 150))}</strong></div></li>`).join('')}</ol><div class="focus-stars">${decorativeArc('focus-arc')}${sparkle('focus-star a')}${sparkle('focus-star b')}${sparkle('focus-star c')}</div></div>${footer(domain,preparedDate,2,totalPages)}</section>`,
`<section class="report-page pages-page">${topbar('Pages reviewed')}<h2>The ${insights.length} Pages Reviewed</h2><p class="hero-subhead">Each reviewed page is scored individually, then interpreted in the context of the full customer journey so the recommendations work together.</p><div class="page-card-grid">${pageCards}</div>${footer(domain,preparedDate,3,totalPages)}</section>`,
`<section class="report-page insights-page">${topbar('Cross-page insights')}<h2>Cross-Page Insights</h2><p class="hero-subhead">This is where the comprehensive tier becomes especially valuable: it reveals site-wide patterns that a one-page audit would naturally miss.</p><div class="insight-grid">${insightCards}</div><div class="why-tier">${notchedFrame('why-frame')}<div class="box-heading">${sparkle('box-spark')}Why this tier matters</div><p>Reviewing multiple pages makes it possible to distinguish between a one-off issue and a repeatable site pattern — so the recommendations become more strategic, more confident, and more useful.</p></div>${footer(domain,preparedDate,4,totalPages)}</section>`,
`<section class="report-page roadmap-page">${topbar('Strategic roadmap')}<h2>Your Strategic Roadmap</h2><p class="hero-subhead">A prioritized plan focused on the improvements most likely to elevate clarity, discoverability, and conversion across the site.</p><div class="roadmap-list">${roadmap || '<p class="empty-note">No critical issues were identified. Continue refining the strongest opportunities in the recommendations section.</p>'}</div><div class="takeaway">${notchedFrame('takeaway-frame')}${sparkle('takeaway-spark')}<div><strong>Focus. Alignment. Momentum.</strong><p>The most valuable fixes are the improvements that make the whole site feel more coherent from first click to final action.</p></div><div class="takeaway-stars">${sparkle('emerald')}${sparkle('navy')}${sparkle('tiny')}</div></div>${footer(domain,preparedDate,5,totalPages)}</section>`,
`<section class="report-page rewrites-page">${topbar('Ready-to-use recommendations')}<h2>Page-by-Page Recommendations</h2><p class="hero-subhead">Polished copy suggestions tied to the pages reviewed, so the client has concrete changes they can implement immediately.</p><div class="rewrite-stack">${rewriteCards || '<div class="empty-note">No copy rewrites were generated for this audit.</div>'}</div>${footer(domain,preparedDate,6,totalPages)}</section>`,
`<section class="report-page technical-page">${topbar('Technical reference')}<h2>Technical Snapshot</h2><p class="hero-subhead">The supporting signals behind your recommendations, summarized for quick reference across the reviewed pages.</p><div class="technical-card"><h3>Page-level snapshot</h3><table><thead><tr><th>Page</th><th>Device</th><th>Performance</th><th>Accessibility</th><th>SEO</th></tr></thead><tbody>${speedRows}</tbody></table></div><div class="signal-grid"><div><strong>${insights.length} pages</strong><span>Pages reviewed</span></div><div><strong>${site.robotsTxt?'Found':'Not found'}</strong><span>robots.txt</span></div><div><strong>${site.hasSitemap?'Found':'Not found'}</strong><span>Sitemap signal</span></div><div><strong>${site.llmsTxt?'Found':'Not detected'}</strong><span>llms.txt<small>Optional future-facing signal</small></span></div></div><div class="method-note">${notchedFrame('method-frame')}<div class="box-heading">${sparkle('box-spark')}How to use this page</div><p>These technical signals support the recommendations, but they do not replace page-level interpretation. The report connects raw findings to messaging, structure, design, and conversion behavior so the next steps feel practical — not merely technical.</p></div><p class="disclaimer">This report is an advisory website assessment based on the information available at the time of review. Recommendations support decision-making and do not guarantee search rankings, revenue, accessibility compliance, or platform eligibility.</p>${footer(domain,preparedDate,7,totalPages)}</section>`
  ];

  return `<!doctype html><html><head><meta charset="utf-8"><style>${css}</style></head><body>${pages.join('')}</body></html>`;
}

const css = `
@page{size:A4;margin:0}*{box-sizing:border-box}p,span,strong,h1,h2,h3,td,th{overflow-wrap:anywhere}html{-webkit-print-color-adjust:exact;print-color-adjust:exact}body{margin:0;color:#17213A;font-family:Arial,Helvetica,sans-serif}.report-page{width:210mm;height:297mm;padding:13mm 15mm 10mm;background:linear-gradient(180deg,#FDFBF7,#FCF9F4);position:relative;overflow:hidden;break-after:page;display:flex;flex-direction:column}.report-page:last-child{break-after:auto}.report-page:before{content:'';position:absolute;width:92mm;height:92mm;border:1px solid rgba(201,121,98,.08);border-radius:50%;right:-43mm;top:-38mm}.spark-svg{fill:currentColor;display:block}.topbar{display:flex;justify-content:space-between;align-items:flex-start;position:relative;z-index:2}.brand-lockup{display:flex;gap:3mm;align-items:center}.brand-icon{width:15mm;height:12mm;position:relative}.brand-icon>svg{width:100%;height:100%}.brand-spark{position:absolute;width:2.2mm;height:2.2mm;right:-2mm;top:4mm;color:#C97962}.brand-name{font:27px Georgia,'Times New Roman',serif;letter-spacing:-.04em}.brand-sara{color:#C97962}.brand-subtitle{font-size:6.5px;text-transform:uppercase;letter-spacing:.22em;margin-top:1mm;font-weight:700}.review-pill{min-height:9mm;padding:2.4mm 5mm;border:1px solid rgba(201,121,98,.75);border-radius:999px;display:flex;gap:2mm;align-items:center;text-transform:uppercase;letter-spacing:.16em;font-size:7px;font-weight:700}.mini-spark{width:2.5mm;height:2.5mm;color:#C97962}.prepared-line{margin-top:16mm;text-transform:uppercase;letter-spacing:.18em;color:#48618B;font-size:7.7px;font-weight:700}.prepared-line:after{content:'';display:block;width:51mm;border-top:1px solid rgba(201,121,98,.65);margin-top:2mm}.cover-grid{display:grid;grid-template-columns:1fr 58mm;gap:10mm;align-items:center;margin-top:11mm}.script-accent{font-family:'URW Chancery L','Apple Chancery','Segoe Script','Brush Script MT',cursive;font-style:italic;color:#C97962;font-size:31px;line-height:1;margin-bottom:2mm}.cover-page h1,.report-page h2{font:700 48px Georgia,'Times New Roman',serif;line-height:.98;letter-spacing:-.035em;margin:0}.cover-page h1{font-size:52px}.ornament-rule{width:68mm;display:flex;align-items:center;gap:2mm;margin-top:4mm}.ornament-rule span{height:1px;flex:1;background:rgba(201,121,98,.7)}.ornament-spark{width:2.5mm;height:2.5mm;color:#C97962}.cover-subtitle,.hero-subhead{font:11.6px/1.55 Georgia,'Times New Roman',serif;max-width:136mm;margin:3mm 0 0}.site-url{font:italic 12px Georgia,'Times New Roman',serif;color:#48618B;margin-top:4mm}.cover-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:2mm;margin-top:5mm}.cover-stats>div{border:1px solid #DED2CB;border-radius:999px;background:#fff;padding:2.2mm;text-align:center}.cover-stats strong{font:700 14px Georgia,'Times New Roman',serif;color:#1F6B58;display:block}.cover-stats span{font-size:5.8px;text-transform:uppercase;letter-spacing:.12em;font-weight:700}.cover-side{text-align:center}.hero-score{width:54mm;height:54mm;position:relative;margin:auto;color:#C97962;display:flex;align-items:center;justify-content:center}.score-gauge{position:absolute;inset:0;width:100%;height:100%;overflow:visible}.gauge-track,.gauge-progress{fill:none;stroke:currentColor;stroke-linecap:round;stroke-dasharray:.1 5}.gauge-track{stroke-width:1.2;opacity:.6}.gauge-progress{stroke-width:1.9}.score-gauge g:not(.gauge-companion){fill:currentColor}.gauge-companion{fill:#17213A}.hero-score>strong{font:52px Georgia,'Times New Roman',serif;font-weight:400;z-index:1}.hero-score-label{display:block;text-transform:uppercase;letter-spacing:.13em;font-size:7px;font-weight:700;margin-top:1mm}.seal{width:42mm;height:42mm;border-radius:50%;background:#0D5D4D;color:white;border:1.5px solid #B58A4B;box-shadow:inset 0 0 0 1.5mm #0D5D4D,inset 0 0 0 1.8mm #D3B466;margin:7mm auto 0;display:flex;flex-direction:column;align-items:center;justify-content:center}.seal span{text-transform:uppercase;letter-spacing:.14em;font-size:6px}.seal b{font:32px Georgia,'Times New Roman',serif;margin:.5mm 0}.notched-frame{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;display:block;overflow:visible}.notched-frame path{fill:rgba(255,255,255,.18);stroke:rgba(201,121,98,.55);stroke-width:.7;vector-effect:non-scaling-stroke}.executive-box{position:relative;padding:7mm 8mm;margin-top:10mm;min-height:57mm}.executive-box>*:not(.notched-frame){position:relative;z-index:1}.box-heading{display:flex;align-items:center;gap:2mm;text-transform:uppercase;letter-spacing:.16em;font-size:8px;font-weight:700}.box-spark{width:3.2mm;height:3.2mm;color:#C97962}.executive-box p,.why-tier p,.method-note p,.takeaway p{font:11.3px/1.55 Georgia,'Times New Roman',serif;margin:3mm 0 0;max-width:150mm}.executive-stars{position:absolute!important;right:6mm;top:7mm;width:18mm;height:22mm}.executive-stars .large{position:absolute;width:8mm;height:8mm;color:#C97962}.executive-stars .small{position:absolute;width:3mm;height:3mm;right:0;top:10mm}.executive-stars .tiny{position:absolute;width:2mm;height:2mm;right:8mm;top:15mm;color:#C97962}.navy{color:#17213A}.emerald{color:#1F6B58}footer{margin-top:auto;padding-top:3mm;font:italic 7.5px Georgia,'Times New Roman',serif;color:#405572}.footer-rule{display:flex;gap:2mm;align-items:center;margin-bottom:2mm}.footer-rule span{height:1px;flex:1;background:rgba(201,121,98,.5)}.footer-spark{width:2.5mm;height:2.5mm;color:#C97962}.footer-meta{display:grid;grid-template-columns:1fr 1fr 1fr}.footer-meta span:nth-child(2){text-align:center;font:600 6px Arial,sans-serif;letter-spacing:.16em}.footer-meta span:last-child{text-align:right}.report-page h2{margin-top:12mm;font-size:43px}.chip-row{display:flex;gap:2mm;flex-wrap:wrap;margin-top:4mm}.page-chip{padding:1.3mm 3mm;border:1px solid #DCCAC1;border-radius:999px;background:#fff;font-size:6.5px;text-transform:uppercase;letter-spacing:.12em;font-weight:700}.score-legend{display:flex;gap:3mm;align-items:center;margin:7mm 0 5mm;font-size:6.4px;text-transform:uppercase;letter-spacing:.1em;font-weight:700}.score-legend span{display:flex;gap:1.5mm;align-items:center}.score-legend i,.score-card-status i,.page-card-status i{width:2mm;height:2mm;border-radius:50%;display:inline-block}.excellent i{background:#0D6A4A}.strong i{background:#2C8264}.foundation i{background:#7F94B5}.opportunity i{background:#B58A4B}.score-grid{display:grid;grid-template-columns:repeat(5,1fr);gap:2.3mm}.score-card{height:51mm;border:1px solid #D9D4CE;border-radius:2.5mm;padding:4mm 2mm 3mm;text-align:center;background:rgba(255,255,255,.45)}.score-card.excellent,.score-card.strong{color:#0D6A4A;border-color:#C5D8CD}.score-card.foundation{color:#6B85A7;border-color:#CCD6E2}.score-card.opportunity{color:#A87935;border-color:#DFCBAA}.score-orbit{height:25mm;position:relative;display:flex;align-items:center;justify-content:center}.score-orbit .score-gauge{width:25mm;height:25mm;left:50%;top:50%;transform:translate(-50%,-50%)}.score-orbit strong{font:31px Georgia,'Times New Roman',serif;font-weight:400;z-index:1}.score-card-label{color:#17213A;font-size:6.8px;text-transform:uppercase;letter-spacing:.1em;font-weight:700;min-height:10mm;display:flex;align-items:center;justify-content:center}.score-card-status{display:flex;gap:1mm;align-items:center;justify-content:center;color:#4F5B6E;text-transform:uppercase;letter-spacing:.07em;font-size:6.1px;font-weight:700}.focus-panel{position:relative;display:grid;grid-template-columns:42mm 1fr 24mm;gap:6mm;align-items:center;padding:7mm 8mm;margin-top:7mm;min-height:57mm}.focus-panel>*:not(.notched-frame){position:relative;z-index:1}.focus-frame path,.why-frame path,.method-frame path{fill:#EEF3EE;stroke:#BFD0C2}.focus-title{font:700 12px Georgia,'Times New Roman',serif;text-transform:uppercase;letter-spacing:.1em;text-align:center;border-right:1px solid #CEC7C1;padding-right:5mm}.focus-panel ol{list-style:none;padding:0;margin:0}.focus-panel li{display:grid;grid-template-columns:7mm 1fr;gap:3mm;margin:3mm 0;align-items:start}.focus-panel li>span{width:6mm;height:6mm;border-radius:50%;background:#C97962;color:#fff;display:flex;align-items:center;justify-content:center;font-size:6px;font-weight:700}.focus-panel li strong{font:11px Georgia,'Times New Roman',serif;font-weight:700}.focus-stars{position:relative;height:30mm}.decorative-arc{position:absolute;fill:none}.decorative-arc path{fill:none;stroke:currentColor;stroke-width:1.3;stroke-linecap:round;stroke-dasharray:.1 5}.focus-arc{width:31mm;height:31mm;right:-7mm;bottom:-4mm;color:#C97962}.focus-star{position:absolute;color:#C97962}.focus-star.a{width:8mm;height:8mm;right:1mm;top:2mm}.focus-star.b{width:3mm;height:3mm;right:0;bottom:6mm;color:#17213A}.focus-star.c{width:2.3mm;height:2.3mm;left:1mm;bottom:0}.page-card-grid{display:grid;grid-template-columns:1fr 1fr;gap:4mm;margin-top:8mm}.page-card{position:relative;padding:5mm 5.5mm;height:50mm;min-height:0;overflow:hidden}.page-card.featured{grid-column:1/-1;height:45mm;min-height:0}.page-card>*:not(.notched-frame){position:relative;z-index:1}.page-card-head{display:flex;justify-content:space-between;gap:5mm;min-width:0}.page-card-kicker{font-size:6.4px;text-transform:uppercase;letter-spacing:.16em;color:#48618B;font-weight:700}.page-card h3{font:700 21px Georgia,'Times New Roman',serif;margin:1mm 0}.page-card-head p{font-size:7.7px;color:#536178;max-width:92mm;margin:0}.mini-gauge{width:23mm;height:23mm;position:relative;display:flex;align-items:center;justify-content:center;flex:none}.mini-gauge .score-gauge{width:23mm;height:23mm;left:50%;top:50%;transform:translate(-50%,-50%)}.mini-gauge strong{font:24px Georgia,'Times New Roman',serif;font-weight:400;z-index:1}.mini-gauge.excellent,.mini-gauge.strong{color:#0D6A4A}.mini-gauge.foundation{color:#6B85A7}.mini-gauge.opportunity{color:#A87935}.page-card-body{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:5mm;margin-top:3mm;min-width:0}.page-card-body span{font-size:6px;text-transform:uppercase;letter-spacing:.14em;color:#1F6B58;font-weight:700}.page-card-body p{font-size:7.6px;line-height:1.45;color:#344256;margin:1mm 0 0}.page-card-status{display:flex;align-items:center;gap:1mm;text-transform:uppercase;letter-spacing:.08em;font-size:6.1px;margin-top:3mm;color:#536178}.insight-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);grid-template-rows:repeat(2,62mm);gap:4mm;margin-top:7mm}.insight-card{position:relative;padding:5.5mm 6mm;height:62mm;min-height:0;overflow:hidden;display:flex;flex-direction:column}.insight-card>*:not(.notched-frame){position:relative;z-index:1}.insight-title{display:flex;gap:2mm;align-items:center;font-size:7.7px;text-transform:uppercase;letter-spacing:.14em;font-weight:700}.insight-card>p{font:10.2px/1.42 Georgia,'Times New Roman',serif;margin:2.5mm 0;overflow-wrap:anywhere}.insight-meta{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:4mm;padding-top:2.5mm;border-top:1px solid #E5DDD7;margin-top:auto;min-width:0}.insight-meta strong{display:block;color:#1F6B58;font-size:5.9px;text-transform:uppercase;letter-spacing:.13em;margin-bottom:1mm}.insight-meta span{font-size:6.8px;line-height:1.4;color:#4A586D}.why-tier,.method-note,.takeaway{position:relative;padding:5.5mm 8mm;margin-top:6mm;overflow:hidden}.why-tier>*:not(.notched-frame),.method-note>*:not(.notched-frame),.takeaway>*:not(.notched-frame){position:relative;z-index:1}.roadmap-list{margin-top:7mm}.roadmap-item{display:grid;grid-template-columns:17mm minmax(0,1fr) 21mm;gap:6mm;padding:2.5mm 0 4mm;border-bottom:1px solid rgba(201,121,98,.35);height:39mm;min-height:0;overflow:hidden}.roadmap-number{font:31px Georgia,'Times New Roman',serif;color:#C97962}.roadmap-status{display:inline-flex;align-items:center;gap:1.2mm;border:1px solid currentColor;border-radius:999px;padding:1mm 2.5mm;text-transform:uppercase;letter-spacing:.13em;font-size:6px;font-weight:700}.roadmap-status.impact{color:#1F6B58}.roadmap-status.improve{color:#9A713A}.status-spark{width:2mm;height:2mm}.roadmap-content h3{font:700 15px Georgia,'Times New Roman',serif;margin:2mm 0}.roadmap-cols{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:6mm;min-width:0}.roadmap-cols strong{font-size:6px;text-transform:uppercase;letter-spacing:.13em;color:#1F6B58}.roadmap-cols p{font-size:7.2px;line-height:1.38;color:#344256;margin:.8mm 0 0;overflow-wrap:anywhere}.roadmap-meta{display:flex;justify-content:space-between;margin-top:2mm;font-size:6.4px;color:#5A6678;font-weight:700}.roadmap-art{position:relative;height:30mm}.roadmap-arc{width:28mm;height:28mm;right:-7mm;top:0;color:#C97962}.roadmap-star{position:absolute}.roadmap-star.one{width:7mm;height:7mm;right:0;top:1mm;color:#C97962}.roadmap-star.two{width:2.8mm;height:2.8mm;right:0;bottom:7mm;color:#17213A}.roadmap-star.three{width:2.2mm;height:2.2mm;left:1mm;bottom:0;color:#1F6B58}.takeaway{display:grid;grid-template-columns:12mm 1fr 20mm;gap:5mm;align-items:center;margin-top:auto}.takeaway-spark{width:9mm;height:9mm;color:#C97962}.takeaway strong{font-size:7.5px;text-transform:uppercase;letter-spacing:.14em}.takeaway-stars{position:relative;height:18mm}.takeaway-stars svg{position:absolute}.takeaway-stars .emerald{width:3mm;height:3mm;color:#1F6B58;right:1mm;top:1mm}.takeaway-stars .navy{width:2.5mm;height:2.5mm;color:#17213A;right:0;bottom:5mm}.takeaway-stars .tiny{width:2mm;height:2mm;color:#C97962;left:1mm;bottom:0}.rewrite-stack{display:grid;grid-template-columns:1fr 1fr;gap:4mm;margin-top:7mm}.rewrite-card{position:relative;padding:4.5mm 5mm;height:52mm;min-height:0;overflow:hidden}.rewrite-card>*:not(.notched-frame){position:relative;z-index:1}.rewrite-head{display:grid;grid-template-columns:9mm 1fr 8mm;gap:3mm;align-items:center}.rewrite-head>span{font:24px Georgia,'Times New Roman',serif;color:#C97962}.rewrite-head small{font-size:5.8px;text-transform:uppercase;letter-spacing:.15em;color:#48618B;font-weight:700}.rewrite-head h3{font:700 16px Georgia,'Times New Roman',serif;margin:.5mm 0}.rewrite-spark{width:6mm;height:6mm;color:#C97962}.rewrite-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:2.5mm 5mm;margin-top:2.5mm;min-width:0}.rewrite-grid strong{font-size:5.8px;text-transform:uppercase;letter-spacing:.13em;color:#1F6B58}.rewrite-grid p{font:8.7px/1.35 Georgia,'Times New Roman',serif;margin:.8mm 0 0;overflow-wrap:anywhere}.technical-card{border:1px solid #D9D1C9;background:rgba(255,255,255,.5);padding:6mm;margin-top:8mm}.technical-card h3{font:22px Georgia,'Times New Roman',serif;margin:0 0 4mm}.technical-card table{width:100%;border-collapse:collapse;font-size:7.5px}.technical-card th{text-align:left;text-transform:uppercase;letter-spacing:.12em;font-size:5.8px;color:#798393;border-bottom:1px solid #D9D1C9;padding:2mm}.technical-card td{border-bottom:1px solid #ECE5DF;padding:3mm 2mm}.signal-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:4mm;margin-top:6mm}.signal-grid>div{border:1px solid #D9D1C9;background:rgba(255,255,255,.45);padding:5mm 4mm;min-height:28mm}.signal-grid strong{font:14px Georgia,'Times New Roman',serif;color:#1F6B58;display:block}.signal-grid span{font-size:5.8px;text-transform:uppercase;letter-spacing:.13em;color:#7A8493;display:block;margin-top:1.5mm}.signal-grid small{display:block;text-transform:none;letter-spacing:.02em;margin-top:1mm}.method-note{margin-top:7mm}.disclaimer{font-size:7.2px;line-height:1.5;color:#687284;margin-top:6mm}.empty-note{font:12px Georgia,'Times New Roman',serif;color:#596477;padding:8mm;border:1px solid #DED7D0}
`;
